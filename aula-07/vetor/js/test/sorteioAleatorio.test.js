const EstruturasIteracao = require("../EstruturasIteracao");
const funcao = EstruturasIteracao.sorteioAleatorio;

test("realizar sorteiro de 1.000.000 e encontrar os mais frequentes", () => {
    const msg = "uma mensagem é uma";
    const expected = "uma";
    expect(funcao());
});
